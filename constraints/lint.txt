# Lint tool constraints — single source of truth for CI and local development.
# Update these versions together when upgrading the lint stack.
# See: .github/workflows/ci.yml, .pre-commit-config.yaml, scripts/lint.sh
ruff==0.15.1
yamllint==1.38.0
